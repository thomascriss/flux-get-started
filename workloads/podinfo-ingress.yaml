apiVersion: v1
kind: Ingress
metadata:
  name: ingress
  namespace: demo
  labels:
    app: podinfo
spec:
  rules:
  - http:
    paths:
    - backend:
        serviceName: podinfo
        servicePort: 9898
    host: podinfo.20.65.22.12.nip.io